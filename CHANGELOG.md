# 更新日志

版本号规则：`主版本.次版本.修订号`（语义化版本）。

---

## v1.2.0 · 首个公开发布版

> v1.0 与 v1.1.0 都只在小范围内做过试用、**没有对外发布**（v1.1.0 是内部编号，未单独发版），因此本版 **v1.2.0** 是**第一个公开发布版本**。
> 下面列出的是本版相对 v1.0 内部试用版的主要变化。

### 新增

- **详情页拖放移动文本**：在「正文 / 标题 / 摘要」里选中一段文字，按住左键拖到目标位置松手即可**移动**；按住 `Ctrl`（或 `Alt`）拖动 = **复制**。
  - 拖动时有**竖线指示落点**（空行也会自动贴到该行最前面）；
  - **支持跨块**：正文里的字可以拖到标题 / 摘要，反过来也行，两边都会保存；
  - **拖到详情页的上 / 下边缘会自动滚动**（越靠边滚得越快），可以拖到当前看不见的位置；
  - 松手后文字保持选中、`Ctrl+Z` 可撤销；从其它软件拖文字进来按纯文本插入。
- **开屏动画**：启动、打开项目、备份时显示；**启动很快时会自动不显示**，不会拖慢启动。
- **卡片底色透明**：词条「换颜色 → 背景图」页里的小开关。背景图有透明区域或未铺满时不再露出白边，直接透出画布背景；选图片背景会自动打开、换成纯色会自动关闭。
- **⚙️ 词条默认值**里新增一排常用色块（最左是内置默认黑、最右是自定义选色）。（这些默认值现在都在 ⚙️ 设置最上面的「🖥️ 全局设置」里。）
- **程序图标**：新增 128×128 与 96×96 图标，用于开屏画面与帮助页。
- **帮助文档**：补充「拖放移动文本」等新条目。
- **📜 开源许可**：帮助面板底部新增入口，弹窗里列出协议要点与第三方组件；完整原文随软件一起分发（安装目录下的 `LICENSE`、`THIRD-PARTY-NOTICES.md`，以及 Electron / Chromium 的官方许可汇编）。
- **⚙️ 设置里新增「🖥️ 全局设置 · 对所有书生效」**（默认收起，标题下面一行小字显示当前值）：把以前散在四处的**全局偏好**集中到一块 —— 主题（深 / 浅色）、自动保存开关与间隔、词条背景图默认显示方式、词条粘贴位置、词条默认文字颜色、详情页宽度（新增 300 / 400 / 480 / 560 预设）、🔒 锁定裁剪框。每一项旁边都有「恢复默认」，底部还有「↩ 恢复全部默认设置」（会再确认一次，**只动设置、不碰任何书的内容**）。
- **初始化后顺手清理提示**：执行「🪟 初始化这本书」后，软件会扫一遍整个书库 —— 如果发现**没有任何书在用**的背景图，就顺口问一次要不要清掉它们（只删没人引用的，正在用的和回收站里引用到的都不动）。点「保留」就什么都不发生，以后仍可在 ⚙️ 设置里点「🧹 清理未引用的背景图片」。

### 改进

- **内置教程改名「教程演示」→「使用教程」**：首次启动时自动放进书库的那本说明书，名字更直白了。旧版本升级上来的用户书库里已经有「教程演示」那本，**不会再给你塞一本新的**（内部加了判断）。
- **颜色值变成了能复制 / 粘贴的输入框**：点色块弹出的是**系统取色器**，那是系统/浏览器自己的小窗口 —— 它里面的输入框**不吃剪贴板**（只能手敲数字，Ctrl+C / V / X 都没反应），这个我们改不了。所以软件界面里那串 `#xxxxxx` 从"普通文字"改成了**真正的输入框**：可以选中、复制、粘贴、剪切，改完**回车生效**（`#4a9` 这种简写会自动补成 `#44aa99`，写错了就还原成当前值，Esc 取消）。词条 / 画布背景的颜色弹窗、详情页背景面板、以及 ⚙️ 设置 →「🖥️ 全局设置 → 词条默认文字颜色」那一行，三处都加上了。
- **「🖥️ 全局设置」里把「自动保存」挪到「深色模式」上面**：收起时那行"当前值"小字本来就是先显示自动保存，展开后却是主题在前，两边顺序不一致、看着别扭；现在面板里也按 **自动保存 → 深色模式 → 词条背景图默认显示方式 → 粘贴位置 → 词条默认文字颜色 → 详情页宽度 → 🔒 锁定裁剪框** 排，和help 里那份"全局 7 项"清单、以及两个确认框里的清单也一起对齐了（只调顺序，功能没动）。
- **启动观感**：改为在页面加载前就用内联样式铺好主题底色、并提前读取上次的主题，窗口底色也跟随系统深浅色，避免出现浅色闪底。
- **系统取色器**：取色器移到色块下方；帮助里补充说明可在窗口顶部切换 **RGB / HEX / HSL** 输入方式。
- **详情页超长内容不再撑高**：摘要最多 **4 行**、标题最多 **5 行**，超出部分就在原位滚动查看（上限取"整行"高度，不会出现半行残行）；拖动详情页宽度后自动按新宽度重排。
- **背景图缓存文件名带导入时间戳**：例如 `风景_20260917-0240.jpg`、`风景_裁剪_20260917-0240.jpg`，一眼看出导入顺序；同一张原图反复导入 / 裁剪只会不断新增文件，**绝不覆盖**已有图片；而且名字里始终只保留**一个**时间戳（反复裁剪同一张图不会叠成两三个、也不会越接越长）。
- **背景图裁剪更好懂**：「**🔳 原图**」（原「整张图片」）＝整图入框、结果就是原图本身（像素尺寸都不变），并且**不锁比例**；**打开裁剪弹窗时就是这个状态**，所以什么都不改直接点「裁剪并应用」不会白裁掉一块、也不会把分辨率降一档（以前一打开就是"铺满裁剪区 + 框内缩 80%"，等于悄悄裁掉两成）。「**🖼️ 居中最大**」＝把图片放大到铺满裁剪区：选着比例时框按这个比例**在图片里居中放到最大**，比例是「自由」时框＝图片 ∩ 裁剪区（就裁你现在看到的这一块）；两种情况都**不锁比例**，接着拉边角还是自由的。弹窗右侧的开关改为「**🔒 锁定裁剪框**」（默认开）：锁定后 ① 框只能在图片里（结果不会有透明块）② 图片也拖不出裁剪区、缩到最小只到"整张图刚好显示全"（图片比例和裁剪区对不上时两边会留一点空白，像普通修图软件那样）；解除锁定则回到老行为 —— 框可以框到图片外面（框外那部分在结果里是透明的，用来做"某一块不要图"的背景），图片也能随便拖、随便缩。选择会记住，下次打开还是它。
- **裁剪弹窗的提示精简了**：按钮上悬停的说明（🔒 锁定裁剪框 / 🖼️ 居中最大 / 🔳 原图 / ✕）、裁剪区下方那行提示、以及各个操作后的提示都改短了，不再一句话里塞三件事；**尺寸只在裁剪区下方那一行报一次**（`✂️ 结果 ≈ W × H px`），别处（例如点「🔳 原图」时的提示）不再重复报一遍像素数；「🔓 已经缩到最小：再缩结果就超过 7000px 上限了」也收成「🔓 已缩到最小（结果上限 7000px）」。帮助文档里仍保留完整说明。
- **「本书」和「全局」不再混着说**：「🪟 初始化这本书」的确认框里多了一个**默认勾着**的「同时把全局设置恢复默认」——勾着＝老习惯（这本书 + 全局设置一起回默认），**取消勾选就只重置这本书、全局设置一点都不动**；确认框开头也从"只影响当前这本书"改成"**下面这些**只影响当前这本书"。「📄 重置本书详情页样式」的说明里点明它会把**全局**的详情页宽度恢复成 400px；「🎨 重置本书词条样式」的说明也点明会碰到全局的**默认文字颜色**。
- **打包**：只输出 **NSIS 安装包**；默认安装到 `%LOCALAPPDATA%\Programs\小说设定管理器`（安装向导里仍可更改）；**卸载时会询问是否保留「小说库」数据**（不会静默删书）；安装包同时带上 `docs/`（README 里那几张界面预览图），免得打开包内那份 README 时图片显示不出来。
- **设置面板从偏上出现、内容只往下长**：点 ⚙️ 打开的那份面板内容最长（展开「🖥️ 全局设置」能顶得很高），所以**不论在书库页还是在某本书里**打开，都让它在**偏上**的位置出现（上方留约 6vh）—— 这样展开 / 收起时**只有下边缘在动**，顶边稳稳不动，视线和鼠标都不用跟着跑；改名 / 帮助 / 各种确认框仍然**居中**出现（贴不贴顶是每份弹窗各自带的，不是全局设置）。哪种都一样：内容最多长到窗口的 **85%** 高，超出部分在面板里滚动，不会顶出屏幕。
- **🏷️ 批量重命名不再记住上次的模板与起始序号**：每次打开都是空白 + 起始序号 1（这次是第五章、下次多半不是它，填回反而要改），⚙️ 设置里也一并去掉了「上次用过的重命名模板」这一项。
- **多选复制按编号顺序**：复制多个词条时按编号（1、2、3…10）进剪贴板，粘贴也按这个顺序依次落位；提示会标明「（按编号顺序）」。
- **帮助页的搜索框改成"悬浮"**：往下翻时搜索框和右边的「全部展开」一直浮在正文上方（`position:sticky`），正文从它们**下面**穿过 —— 所以它**一分正文高度都不占**（上一版把它们做成了一条不参与滚动的工具条，白白吃掉约 90px，正文可视高度小了一截）。这两颗悬浮控件**自己不铺任何底色**（不铺白、不铺毛玻璃、不做 `::before` 垫层、也不加那条 1px 分界线）：给它们垫一层"独立背景"就会变成横贯弹窗的一条色带（也就是之前那条白带，还会顺着弹窗内边距越铺越宽）；现在只保留这两颗控件自己的圆角实底「药丸」（搜索框 + 「全部展开」），中间留白透明。路径开关 / 🔧 开发者工具那一排照旧跟着正文一起滚走。
- **「🖥️ 全局设置」和下面「这本书 / 书库」的功能之间加了一条分界线**：上面那块是"改一次、所有书通用"的，下面那些是"只动这本书"的，用一条线分开，一眼能看出是两类。
- **设置面板里的"说明书"收进帮助**：重置那一块原本铺着一段"哪些是全局的、哪两处会碰到全局"的说明，现在这段话整体挪到帮助的「🖥️ 全局设置与初始化」（那里有全局 / 每本书的完整对照），设置面板里只留一句"只影响当前这本书"；哪个按钮会顺带恢复全局设置，就写在那个按钮下面的小字里。
- **名词统一（"首页"退休）**：以前「首页」同时指两个地方 —— 一个是软件一打开看到的书列表那页，一个是书里 🏠 回到的根词条那一层。现在分开了：书列表那页统一叫「**书库页**」（左上角招牌也从「📚 我的小说工作室」改成「**📚 书库**」），书里那一层叫「**词条主页**」（工具栏那颗按钮上写「**🏠 主页**」，鼠标悬停和帮助里写全称「回到词条主页（根词条画布）」）。帮助、提示语、设置里的说法一起对齐（比如「📂 退出」的说明原本写"回到小说列表"，现在写"回到书库页"；重启提醒里的"始终从首页开始"改成"从书库页开始"）。顺带把三个"书"分清楚：**书库**＝所有书的总和，**书库页**＝看这些书的那一页，**小说库**＝它在磁盘上的那个文件夹（`📁 打开库文件夹` 点开的就是它）—— 文件夹名和按钮名都没动。帮助里新增了一节「**📖 名词表**」（书库页 / 小说库 / 词条主页 / 画布 / 词条 / 节点 / 详情页 + 一句设置分类），哪个词指哪儿拿不准可以直接查；顺手把几处"一个东西两个名字"也统一了：同一批设置以前在初始化确认框里叫「界面设置」、在设置面板里叫「全局设置」→ 现在一律叫「**全局设置**」；`连线` → **连接线**；`详情面板` → **详情页**。（术语统一为「**词条**」：书卡统计「📄 词条: N」、删除提示「不能删除根词条」、连接模式的提示也都用"词条"；**唯一保留"节点"的地方是新建词条的默认标题「新节点」** —— 那只是给新卡片起的名字，不是术语（帮助的名词表里也单列了一条说明这一点）。只改叫法，逻辑一行没动。）另外清了一次重复：「⚙️ 设置」和「🖥️ 全局设置与初始化」两节原本各自把"全局设置有哪些、恢复默认怎么用、初始化的勾选框"讲了一遍，现在「⚙️ 设置」只讲每个按钮干什么，**全局 / 本书的归属、恢复默认和那两处例外统一由「🖥️ 全局设置与初始化」一份说清**（那份清单也修正成和设置面板一致的 7 项）。
- **📍 按位置排列更贴合"眼睛看到的行"**：它以前是**逐张比 y 坐标**，而手拖的卡片很难落在同一个高度上 —— 差几像素就会被判成"上面一行"，于是同一排里稍微高一点的那张会窜到最前面（「📍 排列选中」用的也是同一套判断）。现在改成**先按视觉上的行分组**：两张卡在垂直方向重叠超过较小高度的 **50%** 就算同一行，行内按左右、行间按上下；刚好打平（各一半）就归上面那一行。排完之后卡片的落位、间距、界面上的一切都**和以前完全一样**，没有新增设置项、也没有新增任何界面元素，只是先后顺序终于和眼睛看到的一致。


### 修复

- 修复**打开书之后整页文字被"全选"**的问题（工具栏 + 详情页一起被选中，退出到书库连书名也被选上，明明没按任何键）：两个来源都堵了 —— ① `Ctrl+A` 现在**在处理程序最前面**就先掐掉浏览器默认的"全选整页"（以前万一中间哪一步出错/提前返回，默认行为就漏出来了）；② 选区被**重映射**成"整页"时（选区的锚点节点被重建后，边界会挪到父节点上）会在打开书 / 重绘 / 切回书库时自动清掉。输入框和正文里正常选文字不受影响。
- 修复「🪟 初始化这本书」**会连"引用别名"一起清掉**的问题：别名是用户手填的内容（决定引用词条显示成什么名字），不属于"样式"，现在初始化不再动它。
- 修复详情页搜索的**高亮会把"刚打进去的字"一起吃进去**的问题：高亮用的是 CSS Custom Highlight API，里面的 Range 是"活的" —— 在匹配文字**左边**（也就是 Range 的起点）插入字符时，Range 会自动把新字算成自己的一部分（实测：搜 `abc`、在它左边打个 X → 高亮立刻变成 `Xabc`）。以前只有"手动输入"这条才会重算高亮，而且**输入法组合期间还会跳过**（`isComposing`），所以用中文打字时看着就是"明明不是搜索词也被高亮框住"。现在改成每次内容变化后**延后一拍重算**（组合期间也算、组合结束再补一次），而且重算时**不再自动滚动**到匹配处（免得打字时面板跟着跳）。
- 修复详情页查找栏的**「✕ 清空」在"从全局搜索跳转过来"时不出现**的问题：那个 ✕ 只在**手动打字**（input 事件）时才会同步显示，而「📄 正文搜索」跳转、以及切换词条时都是**用代码**把关键词写进搜索框的 —— ✕ 就永远不出来（得自己敲一个字再删掉才现身）。现在这两种情况也会同步 ✕ 的显示状态。
- 修复详情页**滚动条被"锁住"、往上拖又被拉回来**的问题：详情页里拖动选中的文字时，鼠标拖到面板上/下边缘会启动"自动滚动"（每 16ms 推一次滚动条），但这个定时器**只在"下一次拖非文字的东西"那条分支里才被停掉** —— 松手（drop）、拖拽结束（dragend）都没人停它，于是它会一直跑下去：滚动条被它一直推着（你往上拖就被拉回去），**退出书籍再进来也还在**（它是全局定时器），只有重启软件才会消失 —— 这就是"滚动条被锁住、而且有几率出现"的原因。现在松手 / 拖拽结束 / 窗口失焦 / 定时器发现方向已归零都会立刻停掉。（详情页"每个词条各自记住滚动位置、切回来自动恢复"是另一套逻辑，按设计保留、不受影响。）
- 修复详情页**编辑时一行空行会变成两行**的问题：按回车过去是让浏览器自己"新建段落"（生成 `<div><br></div>` 这类块结构），而保存读的是 `innerText` —— 它把"块本身"和"块里的 `<br>`"各算一个换行，于是界面上的一行在存档里变成两行，**切走再切回 / 重开书籍就凭空多出一行空行**（实测：光标停在空行上按一次回车 → 界面 4 行、存档 5 行、切回来 5 行；文末连按 3 次回车 → 界面 6 行、存档 9 行、切回来 8 行）。现在回车统一插入真正的换行（**正文 / 摘要 / 标题**三块都改了），DOM 里只剩真 `\n`，界面 = 存档 = 读回来完全一致（和 Ctrl+Enter、粘贴走同一套结构），`Ctrl+Z` 撤销不受影响。
- 修复详情页**拖动文字时"每行第一个字前面"放不下落点**的问题：光标停在行首时浏览器量出来的矩形落在**上一行末尾**（只有整个正文的第一行正常，因为它左边没有字符），于是落点竖线被画到上一行去 —— 表现就是"只有第一行能拖到文字前面，其他行最多只能拖到第二个字前面"，而且拖到行首时**根本看不到光标**（其实插入是成功的，缺的只是落点提示）。现在行首（含自动折行折出来的行首）一律按"右边那个字的左边缘"来量，竖线准确落在行首。（补：拖到**面板最左那条留白 / 接近边界线**时，drop 事件的目标元素是外层容器 → 以前那里判定"不在可编辑区"就**静默跳过**，表现成"竖线明明画在行首、松手却不动"；现在落点一律以"算出来的插入位置"为准，贴着分界线拖也一样生效，从别处往详情页拖文字进来的那条路也一起修了。）
- 修复书库页「🗑️ 批量删除」里**手点卡片一张张选满之后，「全选」框不会自己勾上**的问题：以前只有点「全选」框本身才会变状态，于是"看着全选完了、框还是空的"，容易以为自己没选中。现在每点一次卡片都重新核对一遍（选满即勾上、少一个即取消勾选），和词条回收站的行为一致。
- 修复详情页在**空行、空标题、空摘要**处点击时光标被画成"超长竖线"的问题。
- 修复详情页正文**搜索 / 高亮 / 跳转**相关的若干问题。
- 修复帮助面板里的「📜 开源许可」被帮助页遮住、得先关掉帮助才能看到的问题（现在二级弹窗正确叠在帮助之上，关掉后自动回到帮助）。
- 修复**摘要里手动换行在重新打开后不见**的问题（现在摘要和标题、正文一样保留换行与连续空格）。
- 裁剪弹窗里的「**↺ 重置**」按钮**删掉**了：它和「🔳 原图」现在是同一个状态（整图入框 + 比例「自由」）；而且它旧的默认框（铺满裁剪区 + 内缩 80%）既没道理、又会顺手把图片裁小一圈。想回到刚打开时的样子，点「🔳 原图」就行。
- 修复设置里「**↩ 恢复上次重置**」**跨书**的问题：重置记录以前是一份全局列表、换书时不清空，于是在 A 书点过重置、到 B 书点「恢复上次重置」，会把 B 书的词条与背景换成 **A 书重置前的样子**。现在记录只属于记下它的那本书：换书 / 打开书时自动清空，恢复前再核对一次归属（对不上就丢弃并提示）。
- 修复裁剪框**比例锁被边界挤变形**的问题：以前框撞到边界时宽、高是各自压缩的，比例锁会失效（"选框固定不住"）；现在整体等比缩回来，比例始终不变。
- 顺手修掉同类"运行期历史跨书"的小问题：打开另一本书时，**前进历史（`→`）**也会清空（以前可能残留上一本书里的位置）。
- 修复裁剪**透明区变白底 + 分辨率被拉低**的问题：以前只要裁剪框整块落在图片里，导出时就**填白底 + 存 JPEG**；于是"把图片缩到最小裁一次（结果里带透明区）→ 再进裁剪"时，上一轮那块透明会被填成白底、还被 JPEG 压一遍，看起来就是"背景图变白 + 变糊"。现在按**框里到底有没有透明像素**决定格式：有透明 → PNG（透明原样保住）；整块都是不透明的图 → JPEG（体积小，画质也从 0.92 提到 0.95）。
- 修复裁剪弹窗里右侧那个开关**一勾就跑到左边**的问题：那一行原本是"自动换行 + 两端对齐"，左边提示文字一变长（选比例 / 勾开关时都会变长），右边的比例按钮和开关就被挤到换行后的左侧。现在改成不换行、让提示自己吸收多余宽度，比例按钮和开关的位置固定不动。
- 窗口最大化 / 还原时不再把裁剪框踹回初始大小：现在图片和框按新旧裁剪区的比例一起挪过去（等比缩放 → 比例锁不变形），框尽量还待在原来那块地方。
- 裁剪弹窗视觉：右上角 `✕` 放大到 34×34 的**圆形按钮**（平时和别的弹窗一样是灰色 —— 常亮红太抢眼 —— 鼠标移上去才变红底、同时转 90° 更好认），裁剪区四角由圆角改成**直角**（圆角会挡住图片的四个角）。
- 修复裁剪弹窗里**滚轮缩放卡顿**的问题（图片大、带透明区域时尤其明显）：以前每个滚轮事件都要整套走一遍（量坐标 → 改 4 个布局属性 → 重排 → 把缩小的整张位图重新画一遍），而触控板 / 高分辨率滚轮滚一下会连发十几个事件，等于一帧里重复十几次；图片被缩到远小于原尺寸之后（＝"缩小过一次"）每次重新缩的代价更大，带透明的 PNG 又走不了"不透明"的快速路径，就一卡一卡的。现在 ① 一串滚轮事件合并成**每帧只落地一次**（累乘总缩放，手感与逐格滚一致；窗口被遮住 / 最小化时 rAF 不来，用 60ms 定时器兜底，不会"滚了没反应"）；② 图片的位置与缩放改用 `transform` 表达（配合 `transform-origin:0 0`），滚轮不再触发重排，缩放交给合成器做。裁剪结果完全不受影响 —— 输出始终取原图的原生像素（`drawImage` 用的是原图位图，跟界面怎么显示无关）。
- 修复「缩小 → 应用 → 再缩小」能让背景图**一次比一次小**的问题：裁剪结果最长边超过 7000px 时，以前会把**整张（含图片内容）一起等比缩小**，于是每裁一轮图片内容就小一圈（越裁越糊、越裁越小）。现在 ① 解锁后的缩小有了下限 —— 再缩结果就要超过 7000px 就停住并提示（「🔓 已经缩到最小……」）；② 万一框拉得太大，超过的部分只砍**多余的透明边**，图片内容始终按原图像素 1:1 画进去；③ 原图自己就超过 7000px 时（拿巨图当背景），把框进来的整块等比缩到 7000px 内 —— 只是缩，框进来的边角一个都不丢（弹窗提示里写明是原图超限）。
- 修复缩放时下方那一排比例按钮**会上下抖一格**的问题：提示文字一变长（选比例、勾开关、结果像素数变大都会变长）就会把比例按钮挤出一个横向滚动条，按钮被顶高一格；现在提示自己吸收多余宽度、那一行高度写死，比例按钮和锁定开关都不动了。
- 修复「🪟 初始化这本书」确认框里的「同时把全局设置恢复默认」**勾选框被撑成一整条灰盒子、文字被挤出弹窗右侧竖着排**的问题：弹窗里的输入框本来会统一拉满一行（`width:100%` + 内边距 + 圆角），勾选框跟着一起被拉满了；现在勾选框固定 **16×16**，样式表里也补了一条兜底规则（以后新加的勾选框不会再踩这个坑）。
- 修复「🖥️ 全局设置」里**「浅色」「开」被挤成竖排（一个字占一行）、同一排几个「恢复默认」还长短不一**的问题：那一行右边的"说明文字 + 开关 + 按钮"整组会被收缩，小按钮又被"整行按钮"的 `width:100%` 撑成按容器算的宽度（各算各的，于是每行都不一样长）；现在右边那组**固定不收缩**、名称**按词断行**，行内小按钮（恢复默认 / 清空 / 插入…）**统一宽度 78px**，分组标题也统一成"左边名称、右边按钮"。
- 修复从设置里点「❓ 哪些是全局的 / 哪些是每本书的」跳进帮助后，**那一节会一直保持展开、下次打开帮助还停在那儿**的问题：现在和用搜索跳过去一样**不留痕** —— 关掉帮助就把展开状态与滚动位置还原。
- 修复「🪟 初始化这本书」**不会把 🔒 锁定裁剪框 恢复成默认（锁定）**的问题：锁定裁剪框这份选择本来就跟自动保存间隔 / 开关、词条背景图默认显示方式、粘贴位置、详情页宽度、默认文字颜色一样是**全局全局设置**（不是每本书一份），可初始化时偏偏漏了它 —— 在裁剪弹窗里解除过锁定的话，初始化之后还是解着锁，接着裁就会裁出透明块。现在：① 初始化会把它一起带回默认的「锁定」（裁剪弹窗里的勾也同步打上）；② **主题**也一并纳入（以前"回默认"单漏主题，同一批设置两种待遇）；③ 这些全局设置集中到设置面板的「🖥️ 全局设置」里，可单独改、可单独或一键恢复默认；④ 初始化确认框多了一个默认勾着的「同时把全局设置恢复默认」，**取消勾选就只重置这本书** —— 不会再出现"我初始化这本书，怎么把自动保存间隔也改了"。
- 帮助页署名里的版本号去掉「（首个公开发布版）」—— 下面那行日期已经写着首次公开发布，不必重复。
- 修复「🎨 重置本书词条样式」**不会清掉词条自己的"背景图显示方式"**的问题：以前只清了背景图本身，词条自己设过的「完整显示 / 拉伸填充」还留着 —— 重置完背景图没了、显示方式却还是老样子，再给这个词条选图时又不按 🖥️ 全局设置里的默认走。现在会一起清掉（回到全局默认）。

### 说明

- 数据采用**便携式**存放：`小说库` 就在程序（可执行文件）同目录，不写注册表、不藏进用户目录。
- **全局设置**（自动保存、主题、词条默认显示方式、粘贴位置、默认文字颜色、详情页宽度、🔒 锁定裁剪框）记在**这台电脑的程序配置里**，不写进书文件：换电脑时拷走「小说库」不会带走它们，在新机器上重设一次即可（想一次回到出厂状态：⚙️ →「🖥️ 全局设置」→「↩ 恢复全部默认设置」）。
- 软件**完全离线**，不联网、不上传任何数据。

---

## v1.0.0 · 内部试用版（未公开发布）

仅在小范围内试用，没有正式发布记录。
